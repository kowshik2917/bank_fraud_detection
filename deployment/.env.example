# ==============================================================================
# Sentinel Intelligent Banking Fraud Detection Platform - Environment Template
# ==============================================================================

# Backend Configuration
PORT=8000
ENVIRONMENT=production

# MongoDB Atlas Database Connection String (Leave empty to use local in-memory fallback)
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/fraud_platform_db?retryWrites=true&w=majority

# Frontend Configuration
VITE_API_URL=http://localhost:8000
